var NavBar = require("../nav-bar");
var Connect = React.createClass({
  render() {
    return(
      <div>
        <NavBar/>
        Connect with service providers placeholder
      </div>
    );
  }
});
module.exports = Connect;
